# Informações do projeto
sonar.projectKey=dialyhome-demoday
sonar.projectName=DailyHome DemoDay
sonar.projectVersion=1.0

# Diretórios de código fonte
sonar.sources=backend/src,front/src/components,front/src/pages,front/src/services,front/src/utils,front/src/assets

# Diretórios de teste
sonar.tests=backend/tests,front/src/test

# Exclusões (não analisar código como produção)
sonar.exclusions=**/node_modules/**,**/venv/**,**/coverage/**,**/uploads/**,**/setupTests.js,**/setup-test-db.js,**/setup.js,front/src/test/**

# Padrões de arquivos de teste (IMPORTANTE: não excluir .test.js e .spec.js aqui)
sonar.test.inclusions=**/test/**/*.js,**/test/**/*.jsx,**/tests/**/*.js,**/tests/**/*.jsx,**/*.test.js,**/*.test.jsx,**/*.spec.js,**/*.spec.jsx

# Cobertura de código (se estiver usando Jest)
sonar.javascript.lcov.reportPaths=backend/coverage/lcov.info,front/coverage/lcov.info

# Configurações específicas por módulo
# Backend (Python + JS)
backend.sonar.sources=backend/src
backend.sonar.tests=backend/tests
backend.sonar.python.coverage.reportPaths=backend/coverage.xml

# Frontend (JavaScript/React)
front.sonar.sources=front/src
front.sonar.tests=front/src/test
front.sonar.exclusions=front/src/test/**

# Encoding
sonar.sourceEncoding=UTF-8

# Linguagem
sonar.language=js